from django.apps import AppConfig
class PolicyGuardConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.policy_guard'
    verbose_name = 'Policy Guard LGPD e Segurança'
